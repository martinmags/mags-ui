// TODO: Create a script that creates a component template with
// base component
// test file
// storybook file
import React from 'react';
import PropTypes from 'prop-types';

export const CopyPasta = ({ ...props }) => {
  return <div></div>;
};

CopyPasta.propTypes = {};
CopyPasta.defaultProps = {};
